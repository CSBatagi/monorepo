# Local PostgreSQL Secrets
# Copy this to .pg_secrets.local and fill in your credentials
# This matches the production .pg_secrets format for consistency

# Main PostgreSQL password (used by POSTGRES_PASSWORD)
POSTGRES_PASSWORD=your_postgres_password

# Optional: Readonly user password (if you create a readonly user)
# POSTGRES_READONLY_PASSWORD=your_readonly_password
