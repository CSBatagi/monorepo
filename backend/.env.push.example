# Backend Environment Variables for Push Notifications
# Copy this to .env and fill in your values

# ============ VAPID Keys (generated via: npx web-push generate-vapid-keys) ============
# Public key is exposed via GET /push/vapid-public-key (frontend fetches it from backend)
VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
VAPID_SUBJECT=mailto:your-email@example.com

# ============ Firebase Admin SDK ============
# Option 1: Service account JSON file path
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/serviceAccountKey.json

# Option 2: For GCP environments, use application default credentials
# (no additional config needed, just ensure the service account has RTDB access)

# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# ============ Existing Backend Config ============
AUTH_TOKEN=your_existing_auth_token
DB_USER=your_db_user
DB_HOST=your_db_host
DB_DATABASE=your_db_name
DB_PASSWORD=your_db_password
