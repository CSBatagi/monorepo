# Local Environment Configuration
# Copy this to .env.local and fill in your actual credentials
# NEVER commit .env.local to git - it contains sensitive information

# PostgreSQL Configuration for docker-compose.local.yml
# Note: You can also use .pg_secrets.local for postgres container (matches production format)
POSTGRES_DB=csdm
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password

# Backend Database Configuration (must match your local PostgreSQL setup)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_password
DB_DATABASE=csdm

# Production Database Access (for generating stats from production)
PROD_DB_HOST=your.production.host
PROD_DB_USER=readonly_user
PROD_DB_PASSWORD=your_prod_readonly_password
PROD_DB_DATABASE=csdm

# Steam Web API Key (optional - for fetching player avatars)
# Get one from: https://steamcommunity.com/dev/apikey
# If not set, a default Steam avatar will be shown
STEAM_API_KEY=your_steam_api_key_here

# Instructions:
# 1. Copy this file: cp .env.local.example .env.local
# 2. Fill in your actual credentials
# 3. Load variables before running scripts:
#    PowerShell: Get-Content .env.local | ForEach-Object { if($_ -match '^([^=]+)=(.*)$') { [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2]) } }
#    Bash: export $(cat .env.local | xargs)
